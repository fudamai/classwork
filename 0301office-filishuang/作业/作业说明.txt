excel_tidy.py
自动生成的工作表（sheet），包含内容中所有的年份。
在最后一行添加平均值函数
将整理过得文件保存为新文件名，新文件名在原文件名后加-1，再加后缀。

word_util.py
支持添加头部标题、副标题、正文，支持用户自定义文件名保存。



51memo，
对ID的赋值方法进行修改，ID在前一个备忘的id基础上进行加一操作。
查询时，必须输入准确的ID，才能找到备忘。
密码文件单独定义，所有用户的密码保存在一个文件中。
配置文件以 用户名.pkl 命名，备忘数据已 用户名.db 命名。
登录时，只验证用户名及密码。配置文件找到就返回‘ok’，找不到就返回找不到，未做异常处理。
备忘编辑的功能选择，定义一个类函数来实现。有异常处置。

问题：输出PDF的函数构建失败，无法使用。
日志关闭以及等级提升只能调整源代码。